#ifndef PLATFORM_SAMD21G18A_UTILS_H
#define PLATFORM_SAMD21G18A_UTILS_H

/** @brief Poll the GCLK register until it is synchronized. */
inline void platform_samd21g18a_poll_gclk_until_synchronized(void);

#endif
